import styled from "styled-components";

const Container = styled.div`
    
`

const Main = () => {
    return (
        <Container>
            Main
        </Container>
    );
};

export default Main;