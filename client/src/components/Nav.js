import styled from "styled-components";

const Container = styled.div`
    background-color: blue;

    position: absolute;

    z-index: 1;
    margin-top: 15px;
`

const Nav = () => {
    return (
        <Container>
            Nav
            
        </Container>
    );
};

export default Nav;